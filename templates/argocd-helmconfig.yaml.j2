apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: argocd
  namespace: kube-system
spec:
  valuesContent: |-
    # Example override knobs; keep or remove as needed
    configs:
      params:
        server.insecure: "true"
    server:
      extraArgs:
        - --insecure
